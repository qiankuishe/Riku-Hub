{
  "name": "@qiankui-sub-cf/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@qiankui-sub-cf/shared": "workspace:*",
    "hono": "^4.6.19"
  }
}
